[build]
  command = "hugo"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.79.1"
  HUGO_ENABLEGITINFO = "true"

[context.production.environment]
  HUGO_ENV = "production hugo --gc --minify"
